{
  "name": "automanual-ai",
  "version": "1.0.0",
  "description": "AutoManual AI - Generate Software Manuals Automatically",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../worker && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"bgBlue.bold,bgGreen.bold\" \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && ..\\venv\\Scripts\\python main.py",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
